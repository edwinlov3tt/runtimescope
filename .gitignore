node_modules/
dist/
*.tsbuildinfo
.DS_Store
*.zip
.playwright-mcp/

# docs/ used to be globally ignored. Now we maintain a phased-work docs
# convention (ADRs, audits, phase reports, handoffs, etc.) per mc-v2's
# pattern. The structured files are tracked; large/sensitive artifacts
# stay ignored explicitly:
docs/transcripts/
docs/ui-update/
docs/.demo-data/
docs/.DS_Store
**/.DS_Store
/target

# Vendored dashboard SPA (build.rs mirrors packages/dashboard/dist here for rust-embed)
crates/collector-core/dashboard/
