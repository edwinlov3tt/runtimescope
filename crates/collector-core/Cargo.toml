[package]
name = "collector-core"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
tokio.workspace = true
serde.workspace = true
serde_json.workspace = true
axum.workspace = true
tracing.workspace = true
rusqlite = { version = "0.40", features = ["bundled"] }
futures-util = "0.3"
subtle = "2"
sha2 = "0.10"
chrono = { version = "0.4", default-features = false, features = ["clock"] }
# Embed the built dashboard SPA (packages/dashboard/dist) into the binary so the
# collector serves /dashboard with no packages/dashboard on disk (M6 Slice A).
# `debug-embed` forces compile-time embedding in debug builds too (the
# conformance harness runs the debug binary from arbitrary CWDs).
rust-embed = { version = "8", features = ["debug-embed"] }

# Unix-only: process-group signalling for the dev-server lifecycle (group-kill
# the whole tree on stop; ESRCH detection for the "already exited" path).
[target.'cfg(unix)'.dependencies]
libc = "0.2"

[dev-dependencies]
# `ServiceExt::oneshot` — drive axum handlers in-process (no bound port) so the
# dev-server success/409 response SHAPES are asserted, not just the 404 paths
# the conformance harness can reach.
tower = { version = "0.5", features = ["util"] }
